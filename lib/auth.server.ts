// Server-only auth helper placeholder

/**
 * When Firestore auth is integrated, replace this with a server-side session
 * verification (cookie check, token validation, or server SDK call).
 */
export async function getServerAuthStatus(): Promise<boolean> {
  // TODO: perform real server-side session validation
  return false;
}
