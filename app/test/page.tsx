export default function TestPage() {
  return (
    <html>
      <body>
        <div style={{padding: 40, fontFamily: 'sans-serif'}}>
          <h1>Server test — page renders</h1>
          <p>If you see this page at <code>/test</code>, the server is serving HTML correctly.</p>
        </div>
      </body>
    </html>
  );
}
